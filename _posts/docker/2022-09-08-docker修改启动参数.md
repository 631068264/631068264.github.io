---
layout:     post
rewards: false
title:     修改docker container启动参数
categories:
    - docker

---



```sh
# get image id
docker ps|grep image name
# container配置位置
cd /var/lib/docker/containers/[CONTAINER ID]

# 修改hostconfig.json或者config.v2.json
可以尝试建一个相同启动参数的容器，然后对比不同的地方作出修改

# docker
systemctl restart docker
```

